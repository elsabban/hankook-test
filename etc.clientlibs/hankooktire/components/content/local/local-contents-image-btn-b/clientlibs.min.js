/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
function main() {
  const hiddenButton = $('.lcibb .tab-content .img-wrap .hidden-tab-button')


  hiddenButton.on('click',(event)=>{
    const targetData = $(event.target).data('part');
    hiddenButton.removeClass('on');
    $(`.lcibb .tab-content .img-wrap .hidden-tab-button[data-part="${targetData}"]`).addClass('on')
    $('.lcibb .img-list').removeClass('on')
    $(`.lcibb .img-list[data-part="${targetData}"]`).addClass('on')
    $('.lcibb .img-wrap-thumb h2').removeClass('on')
    $(`.lcibb .img-wrap-thumb h2[data-part="${targetData}"]`).addClass('on')
    $(`.lcibb .thumb-wrap img`).removeClass('on')
    $(`.lcibb .thumb-wrap img[data-part="${targetData}"]`).addClass('on')
    window.locomotive && window.locomotive.update();
  })

}
window.addEventListener('compFuncRun', main);
})();

// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
// extracted by mini-css-extract-plugin

})();

/******/ })()
;