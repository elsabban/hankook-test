/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
function main() {
  const hiddenButton = $('.lciba .tab-content .img-wrap .hidden-tab-button')

  hiddenButton.on('click',(event)=>{
    const targetData = $(event.target).data('part');
    hiddenButton.removeClass('on');
    $(`.lciba .tab-content .img-wrap .hidden-tab-button[data-part=${targetData}]`).addClass('on')
    $(`.lciba .text-wrap[data-part="${targetData}"]`).addClass('active').siblings().removeClass('active')
    window.locomotive && window.locomotive.update()
  })
}
window.addEventListener('compFuncRun', main);


})();

// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
// extracted by mini-css-extract-plugin

})();

/******/ })()
;