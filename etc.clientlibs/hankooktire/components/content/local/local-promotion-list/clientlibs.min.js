/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
function main() {

  function hideMore(){
    const li = document.querySelectorAll('.lpl .event-list');
    const initialLength = 6;
    if(li.length > initialLength){
      [...li].forEach((ele, index) => {
        console.log(ele, index)
        if(index > initialLength-1){
          ele.classList.add('await')
        }
      });
      window.locomotive && window.locomotive.update();
    }
  }

  function showMore(){
    const btnWrap = document.querySelector('.lpl .btn-wrap')
    const btnMore = document.querySelector('.lpl a.btn');
    btnMore.addEventListener('click', ()=>{
      const awaitList = document.querySelectorAll('.lpl .event-list.await');
      awaitList.forEach(el => {
        el.classList.remove('await');
      })
      btnWrap.classList.add('hide')
      window.locomotive && window.locomotive.update();
    })
  }
  hideMore();
  showMore();
}
window.addEventListener('compFuncRun', main);
})();

// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
// extracted by mini-css-extract-plugin

})();

/******/ })()
;