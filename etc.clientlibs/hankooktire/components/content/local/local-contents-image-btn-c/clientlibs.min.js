/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be isolated against other entry modules.
(() => {
function main() {
  const hiddenButton = $(".lcibc .tab-content .img-wrap .hidden-tab-button");

  hiddenButton.on("click", (event) => {
    const _target = $(event.target);
    const targetData = _target.data("part");
    $(`.lcibc .hidden-tab-button[data-part="${targetData}"]`).addClass('on')
    $(`.lcibc .hidden-tab-button:not([data-part="${targetData}"])`).removeClass('on')
    $(`.lcibc .title-box`).removeClass('on');
    $(`.lcibc .title-box[data-part="${targetData}"`).addClass('on');
    $(`.lcibc .description-box .fomula span`).removeClass("on");
    $(`.lcibc .description-box .fomula span[data-part="${targetData}"]`).addClass("on");
    $(`.lcibc .img-wrap .desc-box span`).removeClass('active')
        $(`.lcibc .img-wrap .desc-box span`).removeClass('active')
        $(`.lcibc .img-wrap .desc-box span[data-part="${targetData}"]`).addClass('active')

    window.locomotive && window.locomotive.update()
  });
}
window.addEventListener('compFuncRun', main);

})();

// This entry need to be wrapped in an IIFE because it need to be in strict mode.
(() => {
"use strict";
// extracted by mini-css-extract-plugin

})();

/******/ })()
;